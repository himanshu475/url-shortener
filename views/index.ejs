<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }
        .container {
            background-color: #fff;
            padding: 2em;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        h1 {
            color: #333;
            margin-bottom: 1em;
        }
        input[type="text"] {
            width: 80%;
            padding: 10px;
            margin-bottom: 1em;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        button {
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
        .short-url-container {
            margin-top: 1.5em;
        }
        #short-url-link {
            font-size: 1.2em;
            color: #007BFF;
            text-decoration: none;
            word-wrap: break-word;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>URL Shortener</h1>
        <form action="/shorten" method="POST">
            <input type="text" name="originalUrl" placeholder="Enter your long URL here" required>
            <button type="submit">Shorten</button>
        </form>
        
        <% if (shortUrl) { %>
            <div class="short-url-container">
                <p>Your shortened URL is:</p>
                <a id="short-url-link" href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
            </div>
        <% } %>
    </div>
</body>
</html>